# Public client configuration only.
# Copy to .env.local and fill values locally.
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=

# Never commit service-role keys, database passwords, or private MCP tokens.
